# ZkCredential Access Hub Environment Variables

# Backend Configuration
BACKEND_PORT=3001
BACKEND_HOST=localhost

# Frontend Configuration
FRONTEND_PORT=3000
FRONTEND_HOST=localhost

# Midnight Network Configuration
MIDNIGHT_NETWORK_URL=https://testnet.midnight.network
MIDNIGHT_RPC_URL=https://rpc.testnet.midnight.network

# Database Configuration (if using external DB)
DATABASE_URL=sqlite://./data/credentials.db

# Security Configuration
JWT_SECRET=your-jwt-secret-key-here
ENCRYPTION_KEY=your-encryption-key-here

# Development Configuration
NODE_ENV=development
LOG_LEVEL=debug

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Contract Configuration
CONTRACT_ADDRESS_CREDENTIAL_REGISTRY=0x...
CONTRACT_ADDRESS_PROOF_VERIFIER=0x...
